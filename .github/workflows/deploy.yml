name: Deploy Full Stack App (Local)

on:
  push:
    branches:
      - main # `main` ブランチにプッシュされたら実行

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # 環境変数の設定
      - name: Set up environment variables
        run: |
          echo "MONGO_URI is set: ${{ secrets.MONGO_URI }}"  # 環境変数を確認

      - name: Authenticate GitHub
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: git remote set-url origin https://x-access-token:${{ env.GH_TOKEN }}@github.com/syuta336/calendar-memo-app.git

      # Node.js のセットアップ
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # フロントエンドビルド
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Deploy frontend (GitHub Pages)
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: frontend/dist
          clean: true

      # バックエンドセットアップ
      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      # バックエンド起動時に MONGO_URI を環境変数として渡す
      - name: Start backend (Production Mode)
        working-directory: backend
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }} # GitHub Secrets から MONGO_URI を渡す
        run: npm run start
